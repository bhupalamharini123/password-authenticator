services:
  - type: web
    name: express-authenticator
    env: node
    plan: free
    buildCommand: cd "3.5 Secrets Project" && npm install
    startCommand: cd "3.5 Secrets Project" && node index.js
